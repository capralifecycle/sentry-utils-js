{
  "name": "@capraconsulting/sentry-utils-js",
  "version": "0.0.1",
  "description": "Utils for monitoring webapps with Sentry",
  "main": "lib/index.js",
  "source": "src/index.ts",
  "scripts": {
    "build": "rimraf lib && microbundle -o lib -f cjs --name sentry-utils --sourcemap true",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "prettier": "prettier --config ./.prettierrc --write 'src/**/*.ts'"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/jest": "24.0.11",
    "jest": "24.5.0",
    "jest-each": "24.5.0",
    "microbundle": "0.11.0",
    "prettier": "1.16.4",
    "rimraf": "2.6.3",
    "ts-jest": "24.0.0",
    "tslint": "5.14.0",
    "tslint-config-prettier": "1.18.0",
    "typescript": "3.3.4000"
  }
}
